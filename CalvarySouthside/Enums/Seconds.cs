namespace CalvarySouthside.Enums
{
    public enum Seconds
    {
        OneMinute = 60,
        OneHour = OneMinute * 60,
        OneDay = OneHour * 24,
        OneWeek = OneDay * 7,
        OneMonth = OneDay * 30,
        OneYear = OneDay * 365
    }
}
